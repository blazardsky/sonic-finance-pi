# 03 — Repeat pagination at the bottom of the list

Status: ready-for-agent

## Problem

Pagination only appears above the table; it should also appear below it so a
user who scrolled the list does not scroll back up.

## Scope

`pages/Expenses.tsx` and `pages/Incomes.tsx`.

## Done when

Both the top and bottom of each list render the same pagination control.
